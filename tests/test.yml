---
- hosts: leafs,spines
  become: yes
  vars_files:
    - vars.yml
  tasks:
    - include: bgp_tests.yml
    - include: interface_tests.yml
    - include: l3_ping_test.yml

- hosts: spines
  become: yes
  vars_files:
    - vars.yml
  tasks:
    - include: anycast_tests.yml
    - include: vxsnd_tests.yml

- hosts: leafs
  become: yes
  vars_files:
    - vars.yml
  tasks:
    - include: bond_tests.yml
    - include: bridge_tests.yml
    - include: clag_tests.yml
    - include: vxrd_tests.yml
